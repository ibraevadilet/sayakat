import 'package:flutter/material.dart';

class DrowerScreen extends StatelessWidget {
  const DrowerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DrowerScreen'),
      ),
    );
  }
}
